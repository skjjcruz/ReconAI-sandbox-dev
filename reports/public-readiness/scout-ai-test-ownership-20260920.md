# Scout AI test source and route correction — September20

Root reproduced5AI-routing failures and1offline-Alex failure after repairing the separate unit-clock issue. The test auto-selected the unrelated C2 sibling named warroom, and the routing expectations predated two accepted changes: home-chat uses standard; per-pick draft reactions use fast (canonicalb03c273 and owning native e4a5c2f). Correct owning-source selection alone reduced the AI failures from5to3; the original logs are retained.

The tests now require the reviewed ai-analyze source from skjjcruz/github.com-skjjcruz-owner-dashboard-dev at aa13193b28c552f977fa36a4683bb3a688d3da4f. Its SHA256 is4bc29e39dca03bd02ca7eaa79061fa3eb862e6693c676f642d76a2bb3dbbd58e, verified identical in the native-auth proposal. Set AI_EDGE_ROOT to that checkout (WARROOM_ROOT remains an explicit compatible alias). The source resolver may discover normal sibling checkout names only when bytes match; missing/different source fails with instructions and never silently skips backend assertions. Future owning AI implementation changes require reviewing/updating tests/fixtures/ai-edge-source.json. Three source-contract cases verify correct, missing and changed inputs.

All existing model, configured-rate, provider, route and preview assertions remain. Normal standard/premium/deep routing remains covered; explicit assertions now cover home-chat standard and pick-analysis fast on both client and server. The rate test describes reviewed configured rates; it is not an up-to-date vendor-price lookup. No AI model, rate constant, prompt, paid service, production policy, backend or workflow was changed.

Independent product reviewer inspected the complete delta and source history/hash, then ran3source guards,13AI routing and4offline-Alex checks: all pass, no material finding. After syncing the current reviewed canonical union1f597d7 (source7b48330), root ran the full14-script npm test under Node20.20.2 with explicit AI_EDGE_ROOT: all passed. Node20 Vite build passed. The reviewed Scout Yahoo11actualcaller/3Chrome cases also pass using that exact canonical union. Logs are under evidence/scout-yahoo-independent/integrated-{node20-broad,node20-build,caller,browser}.log. Earlier failures and Node25 runs are retained.

These AI checks inspect configured source/contracts and offline routing. They do not exercise real model responses, latency, provider rates/availability, charging, or answer quality. A subsequent tooling correction aligns CI's existing unit-only job with the exact shared revision already read by Pages; the pin value is unchanged. Root compared the revision-validation block byte-for-byte with Pages and reran all10Yahoo release-guard cases. Final canonical integration/pin and hosted CI execution remain required. The old Yahoo served-asset manifest remains intentionally unchanged and prevents uncoordinated provider release. Source can be integrated; owning publication, final pins/manifests and deployed full journeys remain open.
