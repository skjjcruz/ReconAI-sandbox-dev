# Scout Yahoo transaction independent review — September20

Reviewer: root, separate review worktree. Reviewed frozen canonical dc1e501 (base b104ce4) and owning Scout client4c6d709 (base8d6cf54). **Bounded source change cleared; no new material finding.** Neither provider release nor full Scout readiness is approved by this review.

The reviewer inspected the actual canonical legacy connect path, shared hydration/transaction-name cache, opaque published-state context and all three changed Scout renderers. Account/session/connection/league/year changes retire the captured state; retry checks the original scope before publishing and does not substitute unrelated LI history. Confirmed empty, initial unavailable and same-context stale history remain distinct. Player/team strings are escaped; known transaction assets remain named even when absent from current rosters. Non-Yahoo caller branches retain their previous behavior.

Independent reruns:5canonical legacy-state groups,18transaction,14initiating-tab protocol,14data-context and21league-validation groups all pass.11actual Scout caller groups pass. Three real Chrome layouts320×740,390×844 and844×390 pass retry, stale retention, verified-empty replacement, source-derived caller rendering, details, team filter/network, reload and actual second-tab account replacement.44px/overflow/page-error assertions pass. The320px history screenshot was inspected. Logs: evidence/scout-yahoo-independent/{shared,caller,browser}.log. Initial invocation used two nonexistent shortened test filenames after the first37groups passed; the corrected invocation ran every named suite and is the saved complete evidence.

These browser cases load the actual provider/helper/caller source in a controlled surrounding shell with API fixtures; they are not a complete signed-in deployed Scout journey or real Yahoo consent/history proof. Pending/draft-pick/multi-season transactions, historical values/winner grades and broader intelligence remain incomplete as explicitly reported by the author. Existing145-test gate still has9baseline-equivalent tier failures; it is not waived. Root identified a separately verifiable historical test mismatch with the canonical owner-approved2026free-season policy and will repair that test context with boundary coverage in a distinct commit, without changing the business rule.

Integration may proceed with the reviewed canonical union and this consumer. Before any provider release: choose the exact final canonical revision, update the owning consumer pin, build, regenerate and independently review the source/served-asset manifest, publish and verify the consumer, and require the existing guard plus schema preflight. No manifest, pin, workflow, hosted state or deployment was changed in this review. Real provider access and upstream publication authority remain outstanding prerequisites.
